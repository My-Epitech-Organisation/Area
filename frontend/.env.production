# Frontend Production Environment
# This is embedded at build time via Dockerfile ARG
# With nginx reverse proxy, all requests go through https://areaction.app/api/
VITE_API_BASE=https://areaction.app/api
