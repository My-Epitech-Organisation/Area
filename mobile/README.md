# Mobile Stub

This container writes a placeholder `client.apk` into `/apk` volume so the web app can serve it at `/apk/client.apk`.

In a real setup, replace this with your Android build producing the APK to the mounted volume.
